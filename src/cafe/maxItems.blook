if (window.location.pathname != "/cafe/shop") alert("This can only be run in the shop");
else stateNode.setState({ items: Object.keys(stateNode.state.items).reduce((obj, item) => (obj[item] = 5, obj), {}) });